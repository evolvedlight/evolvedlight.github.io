---
layout: post
title: Using Showterm with Docker
---

Halfway through setting up a new Django tutorial today, I realised that recording it would be even better. However, I'd already installed packages, and needed a fresh environment.
I could have of course wiped the install, but that might have left some crud on the system. I could have reinstalled ubuntu, but that would have taken time. I could have created a cloud server (digital ocean, etc), but that adds some extra cost and latency. Let's instead do it with docker!

Let's do the following then:

Install docker:

Install showterm:

TODO: Add detail about ruby too
gem install showterm

Run a bash session from docker showterm